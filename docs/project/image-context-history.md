# 图片上下文修复过程（来源待绑定草稿）

2026-09-17，用户报告 Obsidian 引用被“模型未提供可靠图片额度”阻断。定位为 Core 遍历当前历史图片、对缺失的计价接口直接拒绝；另发现 GPT Responses 适配器只支持文字。用户明确取消过滤图片，批准补齐预算、图片发送与压缩验证。

实施：Core 对缺失接口使用中性图片预留；GPT 保存引用、发送时读取图片、对视觉内容单独估算并在成功压缩后接续摘要。原始日志和附件保留。验证结果见 records/verification/image-context.md。这里依据本任务公开消息与工具结果撰写，未伪造原始事件索引。
