# Vault 绑定设计与地图变更报告

日期：2026-09-18。变更类型：需求、接口草案、模块接入说明和公开来源索引。

## 修改范围

- 新增 Vault 单实例绑定、多 Vault 路由、动态端口自动续接、工作区状态和可选依赖设计。
- 提供方合同在 Suite 与 Maintenance 各自维护；补充模块入口、跨项目消费关系及图节点记录关联。
- 沿用会话贴纸归 ThoughtDAG 的最新职责修订；不更新产品版本、发布记录或运行配置。
- 开发历程只保存公开来源定位，不复制会话正文或隐藏推理。

## 验证结果

- 项目地图校验通过：100 条记录，错误与警告均为 0；需求、接口草案和图节点关联均可解析。
- 开发历程索引校验通过：5 项任务、5 份按需来源资产；本次来源覆盖范围与末尾未配对调用已明确说明。
- 两项目本次变更的 25 份 Markdown 共检查 141 个相对链接，全部目标存在。首次检查发现一处跨项目路径多退一级，修正后复查通过。
- 双方以稳定项目 ID 引用的 3 个新增合同已核对存在：Bridge 绑定、公开扩展页、实例有效范围。
- 本项目文档差异格式检查通过。以上检查仅证明文档结构、来源与链接可用，不证明产品行为已实现。

## 验证边界

本次未修改产品代码，未执行产品单元／浏览器测试，未安装插件或重启实例，未操作真实 Vault 或会话。规格中的验收场景是后续实施要求，不能当作已通过。地图已有架构图保留实际结构，只在节点关联中增加标注为待实现的新需求和接口。
